[배포]  기존 저장소 폴더(zodiac-hill-defense, .git 있는 곳)에 이 zip 내용을 덮어쓴 뒤 upload.bat 더블클릭
        → GitHub 푸시 → Render 자동 배포 (1~3분). 새 폴더에 git init 하지 마세요(기록이 날아감).
[테스트] run_local.bat 더블클릭 → 브라우저가 http://localhost:3000 으로 열림 (3D 모델 포함)
        같은 와이파이 폰: http://내PC아이피:3000
        끝낼 때는 검은 창을 닫으면 됨

public/index.html 을 그냥 더블클릭해도 게임은 되지만 3D 모델은 도형으로 나옴.
새 버전 받으면 이 폴더에 덮어쓰고 upload.bat 다시 실행.
